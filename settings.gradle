rootProject.name = 'kpspemu-root'

include(
        'kpspemu-common',
        'kpspemu',
        'kpspemu-js',

        'korau-atrac3plus-common',
        'korau-atrac3plus',
        'korau-atrac3plus-js',
)

def register(String base) {
    findProject(":$base-common")?.projectDir = file("$base/common")
    findProject(":$base")?.projectDir = file("$base/jvm")
    findProject(":$base-js")?.projectDir = file("$base/js")
}

register('kpspemu')
register('korau-atrac3plus')
